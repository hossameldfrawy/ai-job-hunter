# ── Secrets: never commit ────────────────────────────────────────────────
.env
.env.local
*.session
*.session-journal
*.session-shm
*.session-wal
TELEGRAM_STRING_SESSION.txt
assets/master_cv.pdf
assets/cv_profile.json
secrets/

# ── Local state (cloud state lives on the 'bot-state' branch) ────────────
state/*.db
state/*.db-journal
# The vault holds platform passwords, cover letters and recruiter mail. It must
# never reach the public repo -- unlike jobs.db it is NOT synced to bot-state.
state/vault.db
# Playwright persistent profiles: live login cookies for every board.
state/browser_sessions/
secrets/vault.key
# Gmail OAuth: the token IS mailbox access; the client secret
# identifies the Cloud project. Neither may ever be published.
secrets/gmail_token.json
secrets/gmail_client_secret.json
screenshots/
state/*.db-wal
state/*.db-shm
run_report.json
logs/
*.log

# ── Python ───────────────────────────────────────────────────────────────
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
.pytest_cache/
.mypy_cache/
